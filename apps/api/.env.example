# Server
PORT=3001
NODE_ENV=development

# Convex Database
# Get this from your Convex dashboard: https://dashboard.convex.dev
CONVEX_URL=https://your-deployment-name.convex.cloud

# JWT Configuration
# IMPORTANT: Generate a secure random string for production
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Token Expiry Configuration
# Access tokens: Short-lived (1h recommended for security)
# Refresh tokens: Long-lived (30d recommended)
ACCESS_TOKEN_EXPIRY=1h
REFRESH_TOKEN_EXPIRY=30d

# Resend API Key (get from https://resend.com)
RESEND_API_KEY=re_your_resend_api_key

# Email Configuration
FROM_EMAIL=noreply@yourdomain.com

# Frontend URL (for magic links in development)
FRONTEND_URL=http://localhost:3000

# Metrics Configuration
# Enable/disable metrics tracking
METRICS_ENABLED=true

# Metrics output format: console, file, both, none
METRICS_FORMAT=console

# Metrics file path (only used if METRICS_FORMAT is 'file' or 'both')
METRICS_FILE_PATH=./logs/metrics.jsonl
